global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'shadowdb'
    metrics_path: /api/metrics
    static_configs:
      - targets: ['host.docker.internal:3000']  # adjust host/port as needed